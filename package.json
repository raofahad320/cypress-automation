{
  "name": "cypress-automation",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "rao:cypress": "cypress open",
    "cypress:dev": "set CYPRESS_ENVIRONMENT=dev && cypress open",
    "cypress:dev:run": "set CYPRESS_ENVIRONMENT=dev && cypress run",
    "cypress:staging": "set CYPRESS_ENVIRONMENT=staging && cypress open",
    "cypress:staging:run": "set CYPRESS_ENVIRONMENT=staging && cypress run",
    "cypress:uat": "set CYPRESS_ENVIRONMENT=uat && cypress open",
    "cypress:uat:run": "set CYPRESS_ENVIRONMENT=uat && cypress run"
  },
  "author": "RAO FAHAD",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "cypress": "^13.17.0",
    "moment": "^2.30.1"
  }
}
